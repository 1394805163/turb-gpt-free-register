# 上游合并计划 — origin/main @ bdc1891（2026-09-21）

> 工作树：`register_test-worktrees/merge-dryrun-0921`（分支 `codex/merge-upstream-0921-dryrun`，基于 `3d98717`）
> 目标远程：`origin` = myfanhua/turb-gpt-free-register，`fork` = 1394805163/turb-gpt-free-register
> 状态：**预演合并已执行并停在冲突态**（未提交）。本文件是执行计划书。

## 0. 结论

1. **全量合并可行**，不需要挑拣：上游 19 个提交、48 个文件变更（+4034/−361），与我们 151 个提交只冲突 19 个文件 / 38 个冲突块。
2. 冲突分布：**13 个机械冲突**（键表/列表/导入/按钮 id 的并集）＋ **6 个语义冲突**（登录/查活/代理/注册驱动）。
3. 静态体检：合并树 212 个非冲突 `.py` **全部编译通过**、**无重复顶层定义**；唯一失败 `core/skyvern_registration.py` 是**我们自己的历史遗留 bug**（上游同名文件可编译，属顺手修复项）。
4. 策略：**双向保留优先** —— 上游能力即使暂时不用也原样保留（默认关闭），我们已有更优实现的地方以我们的为准并在其基础上叠加上游改进。

## 1. 事实基线（预演证据）

| 项 | 值 |
|---|---|
| 上游新提交 | 19（`64f41f3..bdc1891`，09-05 ~ 09-19） |
| 变更规模 | 48 文件，+4034/−361 |
| 冲突文件 / 冲突块 | 19 / 38 |
| 自动合并成功 | 29 文件（含 `db.py`、`openai_auth.py`、`email_change_service.py`、`twofa_service.py`、`sms_provider.py`、`webui/app.py`、`sentinel_runner.py`） |
| 编译 | 212 个非冲突 .py：211 通过；`skyvern_registration.py` 预存在 SyntaxError（与本合并无关） |
| 重复定义扫描 | 0 |
| 测试框架 | `unittest`（无 pytest），`python -m unittest discover -s tests`，合并树共 102 个测试文件 |
| 新依赖 | `PySocks`（`core/proxy_chain.py` 用，venv 已装，**requirements.txt 未声明**，需补） |

### 1.1 上游新增能力（全部保留，默认不启用）

- 链式代理：`core/proxy_chain.py`（本地 SOCKS5 中继）+ `core/proxy_utils.py` + `PROXY_POOL_UPSTREAM_PROXY` / `PLAN_CHECK_UPSTREAM_PROXY`
- SMSBower/GrizzlySMS 接码：`core/sms_provider.py` 扩展 + `SMSBOWER_*` 配置
- 密码+2FA 协议登录：`codex_oauth._try_password_mfa_login` 一族
- 纯协议注册补强：`openai_auth.register_user / navigate_email_otp_send / request_password_sentinel_bundle / _request_with_proxy_retry`
- 查活完整 Web 登录兜底：`account_liveness._login_via_full_web_flow`、`close_browser_session`、`probe_auth_session`
- 账号页「完整导出」（邮箱/接码API/密码/2fa.run/2FA密钥）+ generic_api 接码链接规整
- Sentinel 指纹升级（screen/Date/plugin/canvas/navigator/`_so`）
- 换绑逻辑优化、Roxy 密码按钮多语言识别与刷新重试、代理 URL 归一化

### 1.2 我们的独有资产（不得丢失）

- 出口：Mihomo controller/透明路由/US 回退、Resin 代理池文件、`REGISTRATION_PROXY_*`（36 个私有 env 键）
- 邮箱：iCloud IMAP 池（`icloud_mail_client/pool`）、OTP 等待会话 `OtpWaitSession`、日志脱敏 `log_safety`
- 注册/查活：`protocol_page`、`classify_liveness_failure`（403/429/5xx 一律 temporary_error）、托管下游短路、补密码 `account_password`、`password_login`、`pipeline_concurrency`
- 下游：chatgpt2api 推送、`downstream_status` 只读同步、导入导出改造
- 运维：`overnight_pipeline`、Linux 部署脚本、`manage.ps1` / 新 `webui.sh`、102 个测试

## 2. 逐文件处置表

### 2.1 机械冲突（并集即可，13 个）

| 文件 | 块 | 处置 |
|---|---|---|
| `config/__init__.py` | 3 | 键名并集（保留我们的 MIHOMO/RESIN/REGISTRATION_*，追加 `PROXY_POOL_UPSTREAM_PROXY`、`PLAN_CHECK_UPSTREAM_PROXY`） |
| `config/codex.py` | 1 | `apply_env_overrides` 字典并集 |
| `config/email.py` | 1 | 同上（保留 `OTP_SINGLE_WAIT`，追加 `OTP_SETTLE_SECONDS`、`GENERIC_API_PROXY`） |
| `config/env_loader.py` | 1 | `SECRET_ENV_KEYS` 并集（保留 MIHOMO/CHATGPT2API，追加 PLAN_CHECK/PROXY_POOL upstream） |
| `config/proxy.py` | 5 | 保留我们的轮换/租约/vs Resin 全量实现；插入上游 `normalize_proxy_url/normalize_proxy_list` 与 `PROXY_POOL_UPSTREAM_PROXY`；结尾归一化调用叠加到我们的 `resolve_proxy_pool` 之后 |
| `core/codex_agent_service.py` | 1 | import 并集 |
| `core/chatgpt_auth.py` | 1 | 保留我们的 `signin_openai(..., prompt=)`，追加上游 `probe_auth_session` |
| `core/roxybrowser_client.py` | 1 | 并集：上游 `_proxy_pool_relay/_proxy_pool_target` + 我们的 `self.proxy` 赋值 |
| `webui/config_editor.py` | 1 | 保留上游 float fallback try/except + 我们的 `list_str_*` 分支 |
| `webui/templates/index.html` | 1 | 按钮 id 并集（我们 `btnSetPasswordV2` + 上游 CPA/完整导出/复制邮箱） |
| `webui.sh` | 1 | 以我们的为准；可选叠加上游 `get_python()` 的 venv/Flask 友好报错 |
| `sentinel/sentinel-runner.js` | 1 | **取上游**（指纹升级）；我方阅读端 `openai_auth.build_sentinel_header` 已同时消费 `_so`/`so`，且带 `_so_invalid` 错误值防护，无需改动 |
| `.env.example` | 1（整档） | 以上游版为骨架，回填我们 36 个私有键（分组：iCloud / Mihomo / Resin / chatgpt2api / PROXY_POOL_FILE / CODEX_OAUTH_*），并补 `PySocks` 说明 |

### 2.2 语义冲突（需人工合并，6 个）

| 文件 | 冲突点 | 处置原则 |
|---|---|---|
| `core/account_liveness.py` | 登录链选择：我们 `_protocol_fast_path`/`has_password_now` 分支 vs 上游 reauth 失败后 `_login_via_full_web_flow` 兜底 | **以上游新链路为主干，保留我方分流**：托管下游短路 → 有 AT 走 reauth（失败且可重试 → 上游 full web flow 兜底）→ 无 AT/有密码走 full web flow；保留 `classify_liveness_failure`、`OtpWaitSession`、脱敏日志、透明路由轮换 |
| `core/live_check_service.py` | 路由/中继元数据 vs 我们的 `_young_account_country_hint`/路由解析 | 保留我们的路由解析与新号国家提示；把上游 `network_route/proxy_used/upstream_proxy_used/...` 字段并入 `result.update`；`relay` 变量仅在启用上游代理时生效（默认空 = 行为不变） |
| `core/chatgpt_plan.py` | 上游 relay + `close_browser_session` vs 我们的重试/路由语义 | 保留我们的重试循环与 route 延迟解析；把上游 `open_plan_check_proxy`/`relay.close()` 接在 `finally`（未配置上游代理时退化为直接代理） |
| `core/cloakbrowser_driver.py` | 代理选择：我们 `pick_registration_proxy`/resin/mihomo vs 上游 `open_proxy_pool_proxy` | **保留我们的选路**；仅接收上游 `proxy_relay` 形参与 `CloakSeleniumDriver(..., proxy_relay=...)`、构造后 `with` 释放；上游链路只在 `CLOAK_USE_PROXY` 且我们选路为空时作为兜底（可配置关闭） |
| `core/codex_oauth.py` | 4 块：上游密码/MFA/步骤识别 vs 我们 `_pass_mfa_challenge` / page_transport sentinel / `_follow_continue_url` | 采用上游 `_submit_email/_submit_email_otp` 返回 dict + 步骤判定函数族；把我们 `_pass_mfa_challenge` 作为 MFA 分支实现接进去（保留脱敏日志）；`run_codex_oauth` 第 4 步按上游「明确进入 OTP 页才轮询邮箱」改造，保留我们的重发与限次 |
| `core/roxy_registration.py` | 5 块：上游刷新重试助手/密码按钮识别 vs 我们的大改结构 | 以上游新增助手函数（`_refresh_after_missing_page_element`、`_MISSING_PAGE_ELEMENT_REFRESH_RETRIES`）替换我们同位置逻辑，保留我们的结构改动与文案；OTP 输入、passwordless 按钮两块以我们的实现为骨架、吸收上游的 `dict` 返回与多语言识别 |

### 2.3 顺手修复（与本合并无关但一并做）

- `core/skyvern_registration.py`：重复关键字参数 `on_email_acquired`（Py3.12 SyntaxError，`main.py` 选 skyvern 驱动即炸）→ 直接采用上游可编译版本。

## 3. 执行阶段

- **P0 备份**：当前 HEAD 推 `fork` 备份分支 `codex/pre-merge-0921-backup`（线上不动 main）。
- **P1 机械冲突 13 个**：按 §2.1 处理，逐文件 `python -m py_compile`。
- **P2 语义冲突 6 个**：按 §2.2 处理；每完成一个文件立即编译 + 相关单测。
- **P3 依赖与配置**：`requirements.txt` 补 `PySocks>=1.7.1`；重建 `.env.example`；`config_editor` 键表补齐上游新键（WebUI 可编辑）。
- **P4 静态验证**：全量 `py_compile` + 重复定义扫描 + `node --check sentinel/sentinel-runner.js` + `python -m unittest discover -s tests`（含上游新增 5 个测试）。
- **P5 运行时验收**（真实环境，低样本）：
  1. WebUI 启动 + 登录 + 账号页渲染（含"密码/完整导出"按钮）；
  2. 3 个存活账号协议查活（1 个有密码+2FA、1 个仅 AT、1 个上游托管下游）；
  3. 1 次注册 dry-run（protocol_page，走真实但最小的流程）；
  4. 导入导出（sub2api/CPA JSON）+ 邮箱池 UI；
  5. 观察 24h 存活率（与合并前基线对比）。
- **P6 收尾**：合并提交（含本计划文件）→ 推 `fork` 分支 → 记录合并 commit；由主人决定何时切换运行实例。

## 4. 验收门槛（全绿才允许切换运行环境）

| 级别 | 门槛 |
|---|---|
| L1 静态 | 全部 .py 编译通过；无重复顶层定义；JS `node --check` 通过 |
| L2 单测 | `unittest` 全量：失败数 ≤ 合并前基线（P0 需先在 HEAD 副本跑一次基线） |
| L3 冒烟 | WebUI 启动、账号列表/邮箱池/日志三页可用；端口 5000 正常 |
| L4 业务 | 查活 ≥3 账号结果正确（含 1 个 confirmed_dead 判定）；注册 1 个走通协议链；导出 JSON 可被下游解析 |
| L5 观察 | 合并后 24h：新注册号存活率 ≥ 合并前水平；查活 403 误判数不上升 |

## 5. 回滚

1. 未提交：`git -C merge-dryrun-0921 merge --abort`，运行中的 `register-v08-credential-refresh` 不受影响。
2. 已提交未切换：保留旧 worktree 原样，随时可回退运行实例（`manage.ps1 -Action start` 指向旧目录）。
3. 已切换：`git revert -m 1 <merge-commit>` 或直接切回旧 worktree + `fork` 备份分支。
4. 全程不 `push --force`、不删旧分支、不动线上 main。

## 6. 风险清单

| 风险 | 影响 | 缓解 |
|---|---|---|
| `account_liveness` 语义合并出错 | 查活误判/存活率下降 | 以上游主干 + 我方分流；L4 用真实存活号验证；保留 `classify_liveness_failure` |
| codex_oauth 双 MFA 实现并存 | OAuth 拿 RT 流程回归 | 统一到上游步骤机 + 我方 MFA 提交实现；跑 `test_mfa_challenge_login.py`、`test_codex_oauth_*` |
| `cloakbrowser_driver` 代理链路分叉 | 注册出口失效（resin/mihomo 失效） | 以我方选路为唯一入口；L4 注册验证出口 IP |
| `.env.example` 重建遗漏 | WebUI 配置项缺失 | 逐键 diff 校验（36 键回填清单） |
| 上游驱动配置（roxy/browser-use/skyvern）误导 | 误切驱动 | 默认值保持我们现状（`REGISTRATION_DRIVER="cloak"`），仅保留键 |
| PySocks 未装 | 链式代理报错 | 补 requirements；venv 已装 |

## 7. 未决问题（待主人定）

1. 合并落点：独立 worktree 完成（推荐）还是直接在当前运行树？
2. 备份推送 fork 是否现在执行？
3. `.env.example` 骨架取上游、回填我方 36 键 —— 是否同意？
4. 上游暂不用的能力（链式中继/SMSBower/roxy 驱动）保留但不启用 —— 是否同意？
5. 切换运行实例的时间窗（今晚实验跑完后？）
6. 顺手修 `skyvern_registration.py` —— 是否纳入本次合并提交？

---

# 附录 A：P2 语义冲突合并草案（代码级预研，2026-09-21）

> 上述 6 个文件的冲突已逐块研读，按"我方主干 + 上游能力挂接"定稿如下；执行时照此落地即可。

## A1 `core/account_liveness.py`（1 块：ours 51 行 vs theirs 34 行）

分支骨架**保留我方全部前置分流**，仅在非 AT 分支吸收上游链路：

```
if force_protocol:            -> _protocol_fast_path 短路            # 我方保留
if LIVE_CHECK_FAST_REFRESH:   -> oauth_refresh 快路径                # 我方保留
if driver in {cloak,cloakbrowser} and not force_protocol:
                              -> run_cloak_liveness_flow             # 我方保留
existing_access_token / has_totp / has_password_now
if existing_access_token and not has_totp:
    try: session_info = _login_via_reauth(...)                       # 我方
    except Exception as reauth_exc:                                  # 上游兜底
        if not _is_retryable_network_error(reauth_exc): raise
        close_browser_session(session)
        session, session_info = _login_via_full_web_flow(email, failed_proxy,
                                    email_source=..., fingerprint_state=task_fingerprint_state)
else:   # 上游把"无 AT"和"有密码"两条分支合一；_login_via_password_or_otp 内部再分流
    session, session_info = _login_via_full_web_flow(email, proxy,
                                email_source=..., fingerprint_state=task_fingerprint_state)
```

- 统一采用上游 `close_browser_session()`（含 relay 清理）替换 `session.session.close()`（`_network_preflight_with_retry` 与 `finally` 两处）
- 保留：`classify_liveness_failure`、`AccountUnusableError` 分支、`OtpWaitSession`、`redact_email/redact_emails`、`rotate_transparent_route`、`task_fingerprint_state`
- 上游把 dead_code 由 `return {"status": "deactivated"}` 改为 `raise AccountUnusableError`，经我方 `classify_liveness_failure` 归一为 `confirmed_dead`，与 9/21 两号结案口径一致 ✔

## A2 `core/codex_oauth.py`（4 块：1 签名 / 1 大块 246 行 / 2 小块）

- 采用上游助手族：`_extract_continue_url` `_extract_factor_id` `_page_type` `_result_text` `_result_has_any` `_is_password_step` `_is_email_otp_step` `_is_mfa_step` `_is_phone_step` `_password_verify` `_mfa_issue_challenge` `_mfa_verify` `_complete_mfa_if_required` `_follow_login_continue` `_try_password_mfa_login`
  - 依赖的 `_post_json`（L782）/`_resp_json`（L793）我方已有，无需移植 ✔
- `_submit_email`：签名取上游 `-> dict`；**保留**我方 `page_transport` 分支（页内 SDK 免 sentinel）
- `_submit_email_otp`：保留我方 `_resp_json` 定义，叠加"Auth 下一步 page/continue"日志与 `return result`
- `run_codex_oauth` 第 4 步：取上游"**仅当 Auth 明确进入 OTP 页才轮询邮箱**"（省邮箱配额、避免把 MFA 页误判成 OTP 页），保留我方重发上限与脱敏日志
- 我方 `codex_oauth._pass_mfa_challenge`（L953）迁移到上游 `_complete_mfa_if_required` 后若无调用方则删除；`account_liveness._pass_mfa_challenge` 是独立实现且有单测覆盖，保留

## A3 `core/cloakbrowser_driver.py`（3 块）

- 我方 `proxy_selection`/`pick_registration_proxy`/resin/mihomo 选路 = **唯一出口入口**（保留）
- 接收上游 `proxy_relay` 形参并透传 `CloakSeleniumDriver(..., proxy_relay=proxy_relay)`；在 `_quit_and_release_seat()` 内追加 `proxy_relay.close()`
- 上游链式仅当 `CLOAK_USE_PROXY=True`、我方选路为空、`PROXY_POOL_UPSTREAM_PROXY` 非空时才启用（默认关闭，行为不变）
- 输出字典并集：保留我方 `proxy_mode/proxy_group/proxy_node/proxy_exit_ip/proxy_selection_ms`，追加上游 `proxy_pool_target`

## A4 `core/chatgpt_plan.py`（3 块）

- 保留我方：route 延迟解析 + 逐次重试 + `_retry_wait_seconds`（Mihomo/Resin 坏出口重试计数）
- 接入上游：`open_plan_check_proxy(route, route["proxy"], timeout=...)` 得 `effective_proxy, relay`；`finally` 内 `close_browser_session(env)` + `relay.close()`；`route_meta` 排除 `upstream_proxy`
- 元数据字段并集（`network_route/proxy_used/upstream_proxy_used/proxy_mode/proxy_fallback_reason`）

## A5 `core/live_check_service.py`（3 块）

- 保留我方：`_young_account_country_hint`、`_resolve_live_check_route`、`method` 透传、`_append_log` 脱敏
- import 并集：`_mask_proxy, open_plan_check_proxy, resolve_plan_check_route`
- `result.update({...})` 并集网络元数据；`relay` 默认 None（不启用链式时行为与现状一致）

## A6 `core/roxy_registration.py`（5 块）

- 采用上游 `_MISSING_PAGE_ELEMENT_REFRESH_RETRIES` + `_refresh_after_missing_page_element()`：邮箱输入框 / OTP 输入框 / "使用密码继续" / 密码框各允许刷新重试 3 次
- OTP 输入：以我方结构为骨架（`_ensure_otp_input_value` + 单框/六格分流），套上游刷新重试
- passwordless 按钮：保留我方"直接返回 DOM node + `missing_passwordless_button`"契约，吸收上游多语言文本与候选遍历
- 保留我方 `_cfg.ROXY_SELENIUM_TIMEOUT` 超时语义与日志文案

---

# 附录 B：执行期安全条款与事故记录（2026-09-21 追加）

## B1 事故记录（与本合并无直接关系，但影响 P4/P5 执行方式）

- 事实：12:06–12:12 期间发现运行中的 WebUI（PID 5728，10:06:53 启动）已停止；`Get-WinEvent` 无 Python 崩溃事件；旧 `logs/webui.stderr.log` 已被 `manage.ps1` 重启截断，无法取证。
- 处置：已用 `.\manage.ps1 -Action start` 重启，新实例 PID 59800、`http://127.0.0.1:5000` 健康。
- 结论：**原因未定**（外部停止或静默退出均可疑）。不影响合并计划，但下列条款必须执行。

## B2 执行条款（P4/P5 强制）

1. **测试前后都要核对运行实例**：跑全量测试前 `manage.ps1 -Action status` 记录 PID，测试后复核；若实例停止，立即重启并定位（测试套件含 `webui.sh stop` 相关用例）。
2. **全量测试禁止使用真实 `.env`**：真实 `.env` 会让部分用例发起真实 iCloud IMAP 轮询（本次已实测到 `[iCloud IMAP] 连接+检索` 日志风暴）。统一用 `.env.example` 生成脱敏 `.env` 后再跑。
3. **`tests/test_linux_deployment.py` 依赖 WSL bash**：其 `webui.sh stop` 用例使用假 `/proc` 根，Windows 上不会触达真实进程；但必须保持脱敏环境运行，避免真实 PID/端口进入扫描面。
4. **可选改进（不在本次合并范围）**：`manage.ps1` 启动前轮转 `webui.stderr.log`，保留上一次退出证据。

## B3 L2 基线协议

- 在 HEAD 的独立副本（`register_test-worktrees/baseline-head-0921`，脱敏 .env）跑 `python -m unittest discover -s tests`，结果作为合并后的对比基线。
- 判定：合并后失败集合 ⊆ 基线失败集合 + 已知上游测试，方可通过。

## B4 L2 基线实测结果（HEAD 3d98717，脱敏 .env，2026-09-21 12:16）

``
Ran 502 tests in 154.174s
FAILED (failures=18, errors=9, skipped=26)
``

已知问题用例（合并后必须 **不新增** 任何项）：

``
ERROR: test_invalid_otp_resends_then_succeeds
ERROR: test_otp_timeout_resends_once_and_continues_waiting
ERROR: test_pick_registration_proxy_accepts_configured_chatgpt_group_name
ERROR: test_push_retains_oauth_persistence_fields_after_liveness
ERROR: test_stale_plan_worker_cannot_overwrite_a_refreshed_token_or_enqueue_it
ERROR: test_static_viewer_refresh_is_coalesced
ERROR: test_successful_plan_check_marks_token_live_and_enqueues_push
ERROR: test_temporary_plan_failure_does_not_clear_existing_login_refresh_requirement
ERROR: test_unauthorized_plan_check_requests_login_refresh_without_marking_dead
FAIL: test_browser_use_failure_failure_success_restarts_otp_only_once
FAIL: test_cloak_driver_uses_cloak_builder_and_never_creates_roxy_profile
FAIL: test_does_not_push_before_successful_liveness
FAIL: test_enabled_push_with_incomplete_config_persists_push_failed
FAIL: test_frontend_distinguishes_fast_token_check_from_otp_login
FAIL: test_invalid_otp_with_logged_in_page_skips_resend
FAIL: test_jobs_rejects_when_resin_gate_is_not_ready
FAIL: test_modern_ui_exposes_icloud_import_and_resin_status
FAIL: test_no_us_node_or_controller_error_never_falls_back_to_direct
FAIL: test_posts_complete_account_and_is_idempotent_for_same_token
FAIL: test_proxy_country_without_named_profile_is_derived_from_geo
FAIL: test_push_response_for_old_token_is_reported_as_stale_not_success
FAIL: test_queued_old_token_fingerprint_cannot_push_a_replacement_token
FAIL: test_registration_save_only_queues_token_plan_check_not_otp_login
FAIL: test_restore_context_requires_exact_email_match
FAIL: test_retries_transient_errors_with_exponential_backoff_without_logging_token
FAIL: test_successful_full_login_liveness_is_recorded_as_otp_method
FAIL: test_transparent_router_mode_needs_no_explicit_proxy_after_us_switch
``

说明：
- 脱敏 .env（取 .env.example）是**有意为之**：真实 .env 会让用例发起真实 iCloud IMAP 轮询与真实下游请求。
- 这批失败含两类：环境/配置依赖型（如 	est_chatgpt2api_push 需下游配置）与既有测试漂移（如 	est_fast_liveness_push 断言的前端文案已改）。
- 判定口径：合并后失败集合 ⊆ 上述集合，且上游新增 5 个测试通过。

---

# 附录 C：灰测结果与修复（2026-09-21 执行）

## C1 灰测三路（子智能体独立执行）

| 路 | 子智能体 | 结论 |
|---|---|---|
| 运行时冒烟 | Aquinas | ✅ 通过：备用端口 5055 冷启动 OK，登录/主页/9 个 JSON 接口全 200，日志零 Traceback，端口释放，线上 5000 经真实 HTTP 复核健康 |
| 语义审计 | Faraday | ⚠️ 发现 3 处问题（详见 C2） |
| 全量回归 | Gibbs | ⚠️ 502→536 用例，净增 8 条失败；剔除 2 条 .env 噪声后净增 6 条（详见 C2） |

## C2 灰测发现与修复（全部已修）

| # | 问题 | 影响 | 修复 |
|---|---|---|---|
| 1 | `core/roxy_registration.py` 调用了不存在的 `_click_continue_with_password_if_present`（解冲突时被误删） | Roxy 注册走到"邮箱验证码页+使用密码继续"分支必 NameError → 失败回滚 | 从 `origin/main` 原样补回 44 行定义 |
| 2 | `_warm_plan_session` 定义了但未接线（上游在 `check_account_plan` 有调用） | 套餐查询缺少登录态预热 | 在 `env` 创建后插入调用 |
| 3 | `config/email.py` 的 `GENERIC_API_PROXY` 上游默认值非空（`http://127.0.0.1:7897`） | generic_api 取码会固定走本机代理，绕过我们的 resin/mihomo 出口选路 | 默认值改回 `""`（保留 env 覆盖能力） |
| 4 | 上游新代码 7 处日志未按我方约定脱敏邮箱（`codex_oauth` ×6、`roxy_registration` ×1） | 账号隐私泄漏风险 | 全部改用 `redact_email(...)` |
| 5 | `core/openai_auth.py` user/register 失败时落响应体原文（可能含 OTP） | 明文 OTP 落日志 | 改记 `body_len` + sha1 指纹 |
| 6 | Windows `core.autocrlf` 使 `sentinel/sdk.js` 检出行尾变 CRLF | 字节级哈希测试在 Windows 必红；Linux 不受影响 | `.gitattributes` 增加 `sentinel/sdk.js -text` 并按索引重写文件 |
| 7 | `core/chatgpt_auth.py` 直接调 `session.navigator_language()`（测试 stub 无此方法；上游自身测试亦红） | 上下文注入被 except 吞掉 | 加 `hasattr` 守卫 |
| 8 | `core/db.py` 引用 `config.email.OMNIMAIL_BASE`（上游同样缺失） | 完整导出"接码API"列静默为空 | `config/email.py` 补 `OMNIMAIL_BASE` 并登记 env 覆盖 |

## C3 最终验收（同一脱敏 .env，两树对齐 `CLOAK_HEADLESS/PLAN_CHECK_WORKERS`）

| 口径 | 结果 |
|---|---|
| 基线（HEAD 3d98717） | Ran 502 / failures=18 / errors=9 / skipped=26 |
| 合并后（最终） | Ran 536 / 失败集合 ⊆ 基线集合（27 条已知问题用例，逐条比对无新增） |
| 上游新增测试（5 个文件 34 例） | 全部通过/无新增失败 |
| 静态 | 227 个 .py 全量编译（唯一 BOM 项为历史遗留、非合并引入）；0 重复顶层定义；`node --check` 两个 sentinel JS 通过 |
| 导入 | 37 个核心模块 import 全通过 |
| 冒烟 | 备用端口冷启动 + 9 接口 200 + 零异常（C1） |

## C4 遗留（非本次合并引入，挂账）

1. `core/mail_password_change.py` 依赖 `bs4`（venv 未装、全仓无调用方）——历史孤立模块。
2. `core/db.py:724` 的 `OMNIMAIL_BASE` 已补配置；若仍要兜底可再加默认基址。
3. `core/codex_oauth._pass_mfa_challenge` / `_follow_continue_url` 为解冲突时有意保留的我方实现，当前无调用方（静默保留，便于回退对照）。
4. `deploy/linux/gunicorn.conf.py` 首字节 BOM（历史遗留）。
