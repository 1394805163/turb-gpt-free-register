# 上游兼容优化 0.9.2 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 将上游低风险的 TOTP 筛选和延迟邮箱领取行为兼容移植到现有 iCloud/Cloak/Resin 注册机，并发布为 `0.9.2`。

**架构：** 继续使用当前 JSON 文件存储和既有注册驱动接口。后端在 `core/db.py` 做 Python 层状态筛选，WebUI 两套模板只增加同一 `totp_status` 参数；浏览器驱动通过可选邮箱分配回调延迟领取素材。

**技术栈：** Python、Flask、现有 Playwright/CloakBrowser 驱动、原生 `unittest`、CodeGraph。

---

### 任务 1：建立 TOTP 筛选回归

**文件：**
- 创建：`tests/test_account_totp_filter.py`
- 参考：`core/db.py`

- [ ] 编写临时账号数据测试 `enabled`、`disabled`、`pending`、`failed` 和空筛选。
- [ ] 运行 `python -m unittest tests.test_account_totp_filter -v`，确认因参数尚未实现而失败。
- [ ] 保留测试只验证筛选结果和快照中的 `totp_enabled`，不访问真实账号池。

### 任务 2：实现后端 TOTP 筛选

**文件：**
- 修改：`core/db.py`
- 修改：`webui/app.py`

- [ ] 增加 `_matches_totp_status_filter`，按 `totp_secret` 和 `totp_setup_status` 判定。
- [ ] 将 `totp_filter` 贯穿 `_filtered_decorated_accounts`、`list_accounts`、`list_accounts_page` 和 `list_account_plan_check_statuses`。
- [ ] 在 `/api/accounts` 与 `/api/accounts/plan-check-status` 接受 `totp_status`、`totp_filter`、`twofa_status` 兼容别名。
- [ ] 运行任务 1 测试并确认通过。

### 任务 3：接入两套账号页筛选控件

**文件：**
- 修改：`webui/templates/index.html`
- 修改：`webui/templates/index_legacy.html`
- 创建：`tests/test_account_totp_filter.py` 中的模板/API 字符串回归断言

- [ ] 在现代和旧版账号工具栏增加 2FA 状态下拉框。
- [ ] 列表加载和轻量状态轮询携带当前筛选值，筛选变化清空选择并回到第一页。
- [ ] 运行模板/API 回归，确认两套模板均包含控件和请求参数。

### 任务 4：延迟浏览器邮箱领取

**文件：**
- 修改：`core/email_provider.py`
- 修改：`core/registration_service.py`
- 修改：`main.py`
- 修改：`core/cloakbrowser_registration.py`
- 修改：`core/browser_use_registration.py`
- 创建：`tests/test_registration_email_allocation.py`

- [ ] 先写测试证明自动邮箱准备阶段返回空邮箱且不调用 `acquire_email`。
- [ ] 运行测试确认红灯。
- [ ] 增加 `acquire_email_after_input` 和驱动回调，只有找到输入框后才领取邮箱。
- [ ] 保持固定邮箱直接复用；协议入口在启动协议认证前领取。
- [ ] 运行延迟领取、已有 iCloud OTP 和注册服务定向测试。

### 任务 5：版本、验证和发布

**文件：**
- 修改：`VERSION`
- 修改：`CHANGELOG.md`

- [ ] 运行定向测试、`python -m compileall -q core config webui main.py tests` 和 `git diff --check`。
- [ ] 运行 `codegraph sync "D:\\Program files\\moe-atelier-main\\register_test-worktrees\\register-v08-credential-refresh"`。
- [ ] 运行 `codegraph status "D:\\Program files\\moe-atelier-main\\register_test-worktrees\\register-v08-credential-refresh"`。
- [ ] 检查 diff 仅包含本计划范围和版本文件，不包含账号数据、环境变量、日志或密钥。
- [ ] 提交 `chore: release v0.9.2`，创建 `v0.9.2` 标签并推送当前 fork 分支和标签。
